#include <queue>
#include <iostream>
#include <vector>

std::vector<bool> visited(10000, false);

std::vector<std::pair<int,int>> f(){

}

int main() {
    int a, b, n;
    std::cin>>a>>b>>n;

    std::vector<std::pair<int,int>> path(10000);

    std::priority_queue<std::pair<int, int>> q;

    while(!q.empty()){

    }
    

}
